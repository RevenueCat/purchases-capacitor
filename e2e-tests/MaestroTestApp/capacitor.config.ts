import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.revenuecat.maestro.e2e',
  appName: 'MaestroTestApp',
  webDir: 'dist',
};

export default config;
